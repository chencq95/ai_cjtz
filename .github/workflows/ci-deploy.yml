name: ci-deploy

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: production-main
  cancel-in-progress: true

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - run: pip install -e ".[dev]"
      - run: pytest -q
      - name: Migration smoke test
        env:
          DMP_DATABASE_URL: sqlite:///data/ci-migration.db
        run: mkdir -p data && alembic upgrade head
      - uses: pnpm/action-setup@v4
        with:
          version: 10
      - uses: actions/setup-node@v4
        with:
          node-version: "22"
          cache: pnpm
          cache-dependency-path: frontend/pnpm-lock.yaml
      - working-directory: frontend
        run: pnpm install --frozen-lockfile && pnpm build
      - run: docker compose config --quiet
      - name: Reject committed runtime secrets
        shell: bash
        run: |
          ! git ls-files | grep -E '(^|/)(\.env|.*\.pem|id_rsa|id_ed25519)$'
          ! git grep -nE '(gho_|github_pat_|BEGIN (RSA |OPENSSH )?PRIVATE KEY)' -- . ':!*.lock' ':!.github/workflows/ci-deploy.yml'

  deploy:
    needs: test
    if: ${{ github.ref == 'refs/heads/main' }}
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - name: Configure SSH
        env:
          SERVER_SSH_KEY: ${{ secrets.SERVER_SSH_KEY }}
          SERVER_HOST_KEY: ${{ secrets.SERVER_HOST_KEY }}
        run: |
          install -m 700 -d ~/.ssh
          printf '%s\n' "$SERVER_SSH_KEY" > ~/.ssh/id_ed25519
          chmod 600 ~/.ssh/id_ed25519
          printf '%s\n' "$SERVER_HOST_KEY" > ~/.ssh/known_hosts
      - name: Deploy fixed commit
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
          DEPLOY_SHA: ${{ github.sha }}
        run: |
          retry() {
            for attempt in $(seq 1 30); do
              "$@" && return 0
              echo "SSH attempt $attempt failed; retrying" >&2
              sleep 2
            done
            return 1
          }
          git bundle create /tmp/ai-cjtz-deploy.bundle main
          retry scp -P 443 \
            -o IdentitiesOnly=yes \
            -o StrictHostKeyChecking=yes \
            -o HostKeyAlias="$SERVER_HOST" \
            -o ConnectTimeout=8 \
            -o ServerAliveInterval=15 \
            -o ServerAliveCountMax=4 \
            /tmp/ai-cjtz-deploy.bundle \
            "$SERVER_USER@$SERVER_HOST:/tmp/ai-cjtz-deploy.bundle"
          retry ssh -p 443 \
            -o IdentitiesOnly=yes \
            -o StrictHostKeyChecking=yes \
            -o HostKeyAlias="$SERVER_HOST" \
            -o ConnectTimeout=8 \
            -o ServerAliveInterval=15 \
            -o ServerAliveCountMax=4 \
            "$SERVER_USER@$SERVER_HOST" \
            "set -e; cd /ai_cjtz; previous_sha=\$(git rev-parse HEAD); git fetch /tmp/ai-cjtz-deploy.bundle main; git checkout --detach '$DEPLOY_SHA'; rm -f /tmp/ai-cjtz-deploy.bundle; PREVIOUS_DEPLOY_SHA=\"\$previous_sha\" DEPLOY_SKIP_FETCH=true DEPLOY_SHA='$DEPLOY_SHA' bash scripts/deploy.sh"
      - name: Validate production and enqueue the first full crawl
        env:
          SERVER_HOST: ${{ secrets.SERVER_HOST }}
          SERVER_USER: ${{ secrets.SERVER_USER }}
        run: |
          retry() {
            for attempt in $(seq 1 30); do
              "$@" && return 0
              echo "SSH validation attempt $attempt failed; retrying" >&2
              sleep 2
            done
            return 1
          }
          retry ssh -p 443 \
            -o IdentitiesOnly=yes \
            -o StrictHostKeyChecking=yes \
            -o HostKeyAlias="$SERVER_HOST" \
            -o ConnectTimeout=8 \
            -o ServerAliveInterval=15 \
            -o ServerAliveCountMax=4 \
            "$SERVER_USER@$SERVER_HOST" \
            "set -e; cd /ai_cjtz; mkdir -p reports; docker compose exec -T api python scripts/production_validate.py --enqueue-full-if-empty | tee reports/production-validation-latest.json"
